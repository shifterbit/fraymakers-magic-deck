// Hitbox stats for Character Template Projectile
{
	projectileSpawn: {
		hitbox0: {
			damage: 1, hitstop: 60, knockbackGrowth: 30, baseKnockback: 30, angle: 80, reversibleAngle: false, directionalInfluence: true, reflectable: true,
				hitEffectOverride: self.getResource().getContent("ice") + "#vfx"
		}
	},
	projectileIdle: {
	},
	projectileDestroy: {
	}
}
